services:
  yolosite:
    build: ./web
    environment:
      HOSTNAME: 0.0.0.0
      PORT: 3000
    env_file: .env
    restart: unless-stopped
    networks:
      - proxy

networks:
  proxy:
    external: true
    name: proxy
